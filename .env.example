# Environment variables for inflamm-debate-fm
# Copy this file to .env and update the values as needed
# 
# The .env file is automatically loaded by python-dotenv when the package is imported.
# This file is git-ignored, so your local configuration won't be committed.

# Data directory configuration
# Set this to a different location for large data files (e.g., on HPC)
# If not set, defaults to <project_root>/data
# INFLAMM_DEBATE_FM_DATA_ROOT=/path/to/large/data/directory

# Models directory configuration
# Set this to a different location for model files (e.g., on HPC)
# If not set, defaults to <project_root>/models
# INFLAMM_DEBATE_FM_MODELS_ROOT=/path/to/models/directory

# Wandb configuration (optional)
# WANDB_ENTITY=your-entity-name
# WANDB_API_KEY=your-api-key

# Specific path overrides (optional)
# These override paths in the config/default.toml file
# INFLAMM_DEBATE_FM_ANN_DATA_DIR=/path/to/anndata
# INFLAMM_DEBATE_FM_EMBEDDINGS_DIR=/path/to/embeddings
# INFLAMM_DEBATE_FM_COMBINED_DATA_DIR=/path/to/combined/data
